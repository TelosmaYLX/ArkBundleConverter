<Window x:Class="ArkBundleConverterGUI_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArkBundleConverterGUI_WPF"
        mc:Ignorable="d"
        Title="Bundle Converter GUI(Only supports Ark ab files of ver 25.04)" Height="550" Width="600" MinHeight="400" MinWidth="450">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Input Selection Row -->
            <RowDefinition Height="Auto"/>
            <!-- Output Selection Row -->
            <RowDefinition Height="Auto"/>
            <!-- Start Button Row -->
            <RowDefinition Height="Auto"/>
            <!-- Log Label Row -->
            <RowDefinition Height="*"/>
            <!-- Log TextBox Row (takes remaining space) -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Labels -->
            <ColumnDefinition Width="*"/>
            <!-- TextBoxes -->
            <ColumnDefinition Width="Auto"/>
            <!-- Buttons -->
        </Grid.ColumnDefinitions>

        <!-- Input Selection -->
        <Label Grid.Row="0" Grid.Column="0" Content="输入源:" VerticalAlignment="Top" Margin="0,5,5,0"/>
        <TextBox x:Name="txtInputPaths" Grid.Row="0" Grid.Column="1" Margin="0,5,5,5" IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" MaxHeight="100" Text="(未选择)" VerticalContentAlignment="Top"/>
        <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Vertical" VerticalAlignment="Top" Margin="0,5,0,0">
            <Button x:Name="btnSelectFiles" Content="选择文件..." Margin="0,0,0,5" Padding="5,2" Click="BtnSelectFiles_Click"/>
            <Button x:Name="btnSelectInputDir" Content="选择目录..." Padding="5,2" Click="BtnSelectInputDir_Click"/>
        </StackPanel>

        <!-- Output Selection -->
        <Label Grid.Row="1" Grid.Column="0" Content="输出目录:" VerticalAlignment="Center" Margin="0,5,5,0"/>
        <TextBox x:Name="txtOutputDir" Grid.Row="1" Grid.Column="1" Margin="0,5,5,5" IsReadOnly="True" Text="(未选择)" VerticalContentAlignment="Center"/>
        <Button x:Name="btnSelectOutputDir" Grid.Row="1" Grid.Column="2" Content="选择输出目录..." Padding="5,2" VerticalAlignment="Center" Margin="0,5,0,5" Click="BtnSelectOutputDir_Click"/>

        <!-- Start Button -->
        <Button x:Name="btnStart" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Content="开始转换" Margin="0,15,0,10" Padding="10,5" HorizontalAlignment="Center" FontWeight="Bold" Click="BtnStart_Click"/>

        <!-- Log Output -->
        <Label Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Content="日志:" Margin="0,5,0,2"/>
        <TextBox x:Name="txtLog" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" Margin="0,0,0,0"/>

    </Grid>
</Window>